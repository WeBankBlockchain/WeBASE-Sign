<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.webank.webase.sign.api.dao.UserDao">
  <update id="createTbUser">
        CREATE TABLE IF NOT EXISTS tb_user (
          user_id int(11) NOT NULL AUTO_INCREMENT COMMENT '用户编号',
          uuid_user varchar(64) NOT NULL COMMENT '用户唯一的业务userId编号',
          address varchar(64) NOT NULL COMMENT '用户地址',
          public_key varchar(256) NOT NULL COMMENT '公钥',
          private_key varchar(256) NOT NULL COMMENT '私钥',
          description varchar(128) DEFAULT NULL COMMENT '描述',
          encrypt_type int NOT NULL COMMENT '加密类型，1：国密；0：ECDSA',
          gmt_create datetime DEFAULT NULL COMMENT '创建时间',
          gmt_modify datetime DEFAULT NULL COMMENT '修改时间',
          PRIMARY KEY (user_id),
          UNIQUE KEY unique_address (address),
          UNIQUE KEY unique_uuid (uuid_user)
    ) ENGINE=InnoDB AUTO_INCREMENT=100001 DEFAULT CHARSET=utf8 COMMENT='用户信息表';
    </update>

  <sql id="USER_COLUMN">
        user_id as userId,
        uuid_user as uuidUser,
        address,
        public_key as publicKey,
        private_key as privateKey,
        description,
        encrypt_type as encryptType
    </sql>

  <insert id="insertUserInfo" parameterType="com.webank.webase.sign.pojo.po.UserInfoPo">
    <selectKey keyProperty="userId" resultType="java.lang.Integer">
      select LAST_INSERT_ID()
    </selectKey>
    insert into tb_user(
    `uuid_user`,
    `address`,
    `public_key`,
    `private_key`,
    `description`,
    `encrypt_type`,
    `gmt_create`,
    `gmt_modify`
    ) values (
    #{uuidUser},
    #{address},
    #{publicKey},
    #{privateKey},
    #{description},
    #{encryptType},
    NOW(),
    NOW()
    )
  </insert>

  <select id="findUserByUuid" resultType="com.webank.webase.sign.pojo.po.UserInfoPo">
    select <include refid="USER_COLUMN"/>
    from tb_user 
    where uuid_user = #{uuidUser}
  </select>

  <select id="findUserByAddress" resultType="com.webank.webase.sign.pojo.po.UserInfoPo">
    select <include refid="USER_COLUMN"/>
    from tb_user
    where address = #{address}
  </select>

  <select id="findUserList" resultType="com.webank.webase.sign.pojo.po.UserInfoPo">
    select <include refid="USER_COLUMN"/>
    from tb_user
    where encrypt_type = #{encryptType}
    order by user_id
  </select>

  <delete id="deleteUserByUuid" parameterType="java.lang.String">
    delete from tb_user where uuid_user = #{uuidUser}
  </delete>
</mapper>